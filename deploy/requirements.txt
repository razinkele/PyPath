# PyPath dependencies are now managed by pyproject.toml in each package.
#
# This file is kept as a reference only. Do NOT use pip install -r requirements.txt.
#
# Instead, install both packages from source:
#   pip install -e packages/pypath          # pypath-ewe (core)
#   pip install -e packages/pypath-shiny    # pypath-shiny (frontend)
#
# See: packages/pypath/pyproject.toml
# See: packages/pypath-shiny/pyproject.toml
